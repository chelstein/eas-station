# EAS Station Requirements
# Copyright (c) 2025 Timothy Kramer (KR8MER)
# Licensed under AGPL v3 or Commercial License
# See LICENSE and LICENSE-COMMERCIAL files
#
# NOTE: PostgreSQL with the PostGIS extension is required for spatial
# functionality, but it must be provisioned in its own container or
# service. These packages only cover the Python client libraries that
# communicate with the external database instance.
#
# Core Flask dependencies (KEEP during migration - both frameworks running in parallel)
Flask==3.1.2
Flask-SQLAlchemy==3.1.1
Flask-Caching==2.3.1
Flask-SocketIO==5.5.1
Flask-WTF==1.2.2  # CSRF protection and form validation
Flask-Limiter==4.1.1  # Rate limiting with Redis/memory backend
Werkzeug==3.1.4

# Core FastAPI and web framework dependencies (NEW - running alongside Flask)
fastapi==0.124.2
uvicorn[standard]==0.38.0  # ASGI server with WebSocket support
python-socketio==5.15.0  # Socket.IO support for both Flask and FastAPI
python-multipart==0.0.20  # Form data and file upload parsing
jinja2==3.1.6  # Template rendering (shared by both frameworks)
itsdangerous==2.2.0  # Secure session cookies and CSRF tokens (shared)
starlette==0.50.0  # Starlette (included with FastAPI, pinning for session support)
slowapi==0.1.9  # Rate limiting for FastAPI (Flask-Limiter equivalent)
aiofiles==25.1.0  # Async file operations

# Database dependencies
SQLAlchemy==2.0.45
psycopg2-binary==2.9.11  # Sync PostgreSQL driver (can migrate to asyncpg later)
GeoAlchemy2==0.18.1
Alembic==1.17.2
greenlet==3.3.0  # Required for SQLAlchemy 2.0 with FastAPI sync operations

# HTTP client for API calls
requests==2.32.5
certifi==2025.11.12  # Updated CA bundle for SSL certificate verification (required for IPAWS)
httpx==0.28.1  # Modern async HTTP client with connection pooling

# Timezone handling (CRITICAL FOR PROPER TIME DISPLAY)
pytz==2025.2

# System monitoring
psutil==7.1.3

# Environment variables
python-dotenv==1.2.1

# Markdown rendering for documentation viewer
mistune==3.1.4  # Fast, extensible markdown parser with full GFM support

# YAML parsing for compose file validation
PyYAML==6.0.3

# JSON handling - Optimized parsers for better performance
orjson==3.11.5  # Fast C-based JSON parser (2-3x faster than stdlib)
ujson==5.11.0  # Alternative fast JSON parser (fallback option)

# Datetime parsing - More robust than manual strptime
python-dateutil==2.9.0.post0  # Flexible datetime string parsing

# XML parsing - Optimized for CAP alert processing
lxml==6.0.2  # Fast C-based XML parser (5-10x faster than ElementTree)

# Document and spreadsheet exports
openpyxl==3.1.5

# Production servers (BOTH during migration)
gunicorn==23.0.0  # WSGI server for Flask (port 5000)
gevent>=25.9.1    # Async worker for Flask-SocketIO with Gunicorn (Python 3.13+ compatible)
# uvicorn (ASGI server for FastAPI on port 8080) already included above with [standard] extras
# uvicorn[standard] includes: websockets, httptools, uvloop, watchfiles

# Redis for state management and worker coordination
# Replaces fragile file-based coordination with robust distributed state
redis==7.1.0
hiredis==3.3.0  # C parser for better Redis performance

# Development and debugging (optional)
# flask-debugtoolbar==0.13.1  # Uncomment for development

# Testing framework (required for web UI test runner)
pytest==9.0.2
pytest-asyncio==1.3.0  # For async test support

# Optional speech synthesis support
# azure-cognitiveservices-speech==1.38.0  # Required when enabling Azure AI voiceovers
pyttsx3==2.99  # Offline text-to-speech engine for the pyttsx3 provider

# Audio processing for EAS decoding
scipy==1.16.3  # Optional: Advanced signal processing (not used for EAS resampling)
pydub==0.25.1  # Audio stream decoding (MP3, AAC, OGG) for HTTP/Icecast streams
# Note: pydub requires ffmpeg to be installed via system packages (libavcodec, libavformat)
# Note: EAS monitor uses numpy's linear interpolation for efficient resampling on Raspberry Pi

# SDR support for radio receivers
numpy==2.3.5  # Required for SoapySDR sample processing
# Note: SoapySDR Python bindings are installed via system packages (python3-soapysdr)

# VFD Display support (Noritake GU140x32F-7000B)
pyserial==3.5  # Serial communication for VFD display
Pillow==12.0.0  # Image processing for VFD graphics rendering

# OLED display support (Argon Industria SSD1306 module)
luma.oled==3.14.0  # I2C SSD1306/SH1106 driver used by the Argon OLED

# GPIO control for Raspberry Pi hardware
gpiozero==2.0.1

# Multi-Factor Authentication (MFA) support
pyotp==2.9.0  # TOTP (Time-based One-Time Password) generation and verification
qrcode==8.2  # QR code generation for MFA enrollment

# Geospatial data processing
pyshp==3.0.3  # Shapefile reader for converting boundary files to GeoJSON
