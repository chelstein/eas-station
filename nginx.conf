# nginx configuration for EAS Station with HTTPS support (DOCKER DEPLOYMENT ONLY)
# This configuration is specifically for Docker Compose deployments.
# For bare metal installations, use config/nginx-eas-station.conf instead.

events {
    worker_connections 1024;
}

http {
    # Use Docker's embedded DNS resolver and disable IPv6 so upstream lookups
    # do not return IPv6-only addresses that the backend container doesn't bind.
    # The valid=30s caches DNS results for 30 seconds to reduce lookup overhead.
    resolver 127.0.0.11 ipv6=off valid=30s;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Rate limiting zone to prevent abuse
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;

    # Backend server address - using a variable forces runtime DNS resolution
    # which respects the resolver's ipv6=off setting. Static upstream blocks
    # resolve at startup and ignore the resolver directive.
    map $host $backend_server {
        default "app:5000";
    }

    # WebSocket connection upgrade map
    # This properly handles both WebSocket upgrades and regular HTTP keepalive connections
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    # HTTP server - redirect to HTTPS (will be enabled after SSL certificates are obtained)
    server {
    listen 8888;
    listen [::]:8888;
    server_name ${DOMAIN_NAME};

    # Allow Let's Encrypt ACME challenges
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Allow local API access over HTTP (for OLED/LED/VFD display scripts on host)
    # These endpoints need to be accessible without HTTPS redirect for local hardware displays
    location ~ ^/(api/|health|ping|version) {
        # Only allow from localhost for security
        allow 127.0.0.1;
        allow ::1;
        deny all;

        # Use variable to force runtime DNS resolution (respects resolver ipv6=off)
        set $backend http://$backend_server;
        proxy_pass $backend;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_buffering off;
    }

    # Redirect all other HTTP traffic to HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS server
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name ${DOMAIN_NAME};

    # SSL certificate paths (managed by certbot)
    ssl_certificate /etc/letsencrypt/live/${DOMAIN_NAME}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN_NAME}/privkey.pem;

    # SSL configuration (Mozilla Intermediate compatibility)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # SSL session settings
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;

    # OCSP stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    ssl_trusted_certificate /etc/letsencrypt/live/${DOMAIN_NAME}/chain.pem;

    # Security headers
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "upgrade-insecure-requests" always;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Client body size limit (for file uploads)
    client_max_body_size 100M;

    # Proxy timeouts
    # Increased to 300s (5 minutes) to accommodate long-running audio decoding operations
    # Audio processing can take 60-180s for large files with SAME detection, tone analysis, and narration extraction
    proxy_connect_timeout 300s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;

    # Proxy to Flask backend
    location / {
        # Apply general rate limiting
        limit_req zone=general_limit burst=50 nodelay;

        # Use variable to force runtime DNS resolution (respects resolver ipv6=off)
        set $backend http://$backend_server;
        proxy_pass $backend;
        proxy_http_version 1.1;

        # Proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # WebSocket support - proper upgrade handling
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Disable proxy buffering for streaming responses
        proxy_buffering off;
    }

    # Audio streaming - proxy DIRECTLY to audio-service container (port 5002)
    # This bypasses the app container for better performance and simplicity
    location /api/audio/stream/ {
        # No rate limiting for audio streams (they're continuous)
        
        # Proxy directly to audio-service container
        set $audio_service http://audio-service:5002;
        proxy_pass $audio_service;
        proxy_http_version 1.1;
        
        # Streaming-specific settings
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Critical: Disable ALL buffering for real-time streaming
        proxy_buffering off;
        proxy_cache off;
        proxy_request_buffering off;
        
        # Long timeout for continuous streaming
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        
        # Add CORS headers for audio playback
        add_header Access-Control-Allow-Origin * always;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    # API endpoints with stricter rate limiting
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;

        # Use variable to force runtime DNS resolution (respects resolver ipv6=off)
        set $backend http://$backend_server;
        proxy_pass $backend;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_buffering off;
    }

    # Static files (if served directly by nginx for better performance)
    location /static/ {
        alias /app/static/;
        expires 5m;
        add_header Cache-Control "public, max-age=300, must-revalidate";

        # Add ETag support for cache validation
        etag on;

        # Allow revalidation when version changes
        add_header X-Content-Type-Options "nosniff" always;
    }
}
}
